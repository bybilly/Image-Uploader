<!DOCTYPE html>
<html lang="en">
<head>
    <title>View Image <%= id %></title>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <meta name="twitter:card" content="photo">
    <meta name="twitter:site" content="@billy_lardner">
    <meta name="twitter:title" content="View Image <%= id %>">
    <meta name="twitter:description" content="Billy's Image Uploader">
    <meta name="twitter:image:src" content="<%= link %>">
    <meta name="twitter:image:alt" content="Image uploaded by a user with ID <%= id %>">

    <meta property="og:site_name" content="Billy's Image Uploader">
    <meta property="og:title" content="View Image <%= id %>">
    <meta property="og:image" content="<%= link %>">
    <meta property="og:description" content="Billy's Image Uploader">
    <meta property="og:url" content="https://uploader/bybilly.uk/<%= id %>">
    <meta property="og:type" content="website">

    <title>Image Uploader - View image <%= id %></title>

    <style>
        * {
            box-sizing: border-box;
        }

        body {
            font-family: Helvetica;
            background: #111;
            color: white;
        }

        h1 {
            font-size: 22px;
            text-transform: uppercase;
        }

        header a {
            text-decoration: none;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 5px 30px;
            font-size: 18px;
            background: #333;
            color: white;
            width: 100%;
            height: 60px;
        }

        img {
            max-width: calc(100% - 10px);
            max-height: 900px;
            border: 5px solid white;
            border-radius: 4px;
            margin: 25px 0 15px 0;
        }

        .main {
            text-align: center;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: calc(100vh - 150px);
        }

        footer {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 25px 30px;
            font-size: 18px;
            color: white;
            width: 100%;
            height: 40px;
        }

        a {
            color: white;
        }

        #link {
            border: 2px solid #111;
            height: 25px;
            width: 260px;
        }
    </style>
</head>
<body>
    <header>
        <h1><a href='/'>Simple Image Uploader</a></h1>
        <p>Share this image: <input id="link" readonly value="https://uploader.bybilly.uk/image/<%= id %>"></p>
    </header>
    <div class="main"><img src="<%= link %>"></div>
    <hr>
    <footer>
        <p>Made by Billy :)</p>
        <p><a href="/">Upload your own image</a></p>
    </footer>
</body>
</html>